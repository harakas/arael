# GENERATED by cargo-arael: shim crate for the `Path` model. Written once -- edit freely; delete the file to regenerate.
[package]
name = "slam-demo-gm-capi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
slam-demo-gm = { path = ".." }
arael = { path = "../../../.." }
