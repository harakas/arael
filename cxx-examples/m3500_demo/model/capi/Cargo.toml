# GENERATED by cargo-arael: shim crate for the `Graph` model. Written once -- edit freely; delete the file to regenerate.
[package]
name = "m3500-demo-capi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
m3500-demo = { path = ".." }
arael = { path = "../../../.." }
