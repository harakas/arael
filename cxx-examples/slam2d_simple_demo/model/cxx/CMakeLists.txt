# GENERATED by cargo-arael: build glue for the `Path` model interface. Do not edit.

#
# Consumer usage:
#   add_subdirectory(path/to/cxx)        # or FetchContent
#   target_link_libraries(app PRIVATE arael::slam2d_simple)
#
# Requires a Rust toolchain (cargo builds the capi staticlib; cargo is
# its own dependency tracker, so the custom target always runs and
# no-ops when nothing changed).
cmake_minimum_required(VERSION 3.16)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
  project(slam2d_simple_capi NONE)
endif()

set(_arael_capi_dir "${CMAKE_CURRENT_SOURCE_DIR}/../capi")
set(_arael_cargo_target "${CMAKE_CURRENT_BINARY_DIR}/cargo")
set(_arael_capi_lib "${_arael_cargo_target}/release/libslam2d_simple_capi.a")

add_custom_target(slam2d_simple_capi_build
  COMMAND cargo build --release
          --manifest-path "${_arael_capi_dir}/Cargo.toml"
          --target-dir "${_arael_cargo_target}"
  BYPRODUCTS "${_arael_capi_lib}"
  VERBATIM)

add_library(arael_slam2d_simple STATIC IMPORTED GLOBAL)
set_target_properties(arael_slam2d_simple PROPERTIES
  IMPORTED_LOCATION "${_arael_capi_lib}"
  INTERFACE_INCLUDE_DIRECTORIES "${CMAKE_CURRENT_SOURCE_DIR}/include")
if(UNIX)
  set_property(TARGET arael_slam2d_simple APPEND PROPERTY
    INTERFACE_LINK_LIBRARIES pthread dl m)
endif()
add_dependencies(arael_slam2d_simple slam2d_simple_capi_build)
add_library(arael::slam2d_simple ALIAS arael_slam2d_simple)
