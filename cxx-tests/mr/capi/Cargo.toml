# GENERATED by cargo-arael: shim crate for the `Decay` / `Line` model. Written once -- edit freely; delete the file to regenerate.
[package]
name = "cxx-mr-capi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
cxx-mr = { path = ".." }
arael = { path = "../../.." }
