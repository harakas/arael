[package]
name = "plane-bench"
version = "0.1.0"
edition = "2021"

[dependencies]
arael = { path = "../.." }
bench-harness = { path = "../harness", features = ["factrs"] }
factrs = "0.3.0"
rand = "0.9"
rand_distr = "0.5"

[workspace]

[profile.release]
debug = false
