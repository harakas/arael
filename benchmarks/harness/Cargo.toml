[package]
name = "bench-harness"
version = "0.1.0"
edition = "2021"
publish = false

# Standalone workspace, like every benchmark that depends on it.
[workspace]

[dependencies]
arael = { path = "../.." }
serde_json = "1"
libc = "0.2"
num = "0.4"
