// -----------------------------------------------------------------------------
// This file was autogenerated by symforce from template:
//     function/FUNCTION.h.jinja
// Do NOT modify by hand.
// -----------------------------------------------------------------------------

#pragma once

#include <Eigen/Core>

namespace sym {

/**
 * Roll/pitch from an accelerometer.
 *     jacobian: (2x6) jacobian of res wrt arg pose (6)
 *     hessian: (6x6) Gauss-Newton hessian for arg pose (6)
 *     rhs: (6x1) Gauss-Newton rhs for arg pose (6)
 */
template <typename Scalar>
void TiltFactor(const Eigen::Matrix<Scalar, 6, 1>& pose, const Eigen::Matrix<Scalar, 2, 1>& tilt,
                const Scalar isigma, Eigen::Matrix<Scalar, 2, 1>* const res = nullptr,
                Eigen::Matrix<Scalar, 2, 6>* const jacobian = nullptr,
                Eigen::Matrix<Scalar, 6, 6>* const hessian = nullptr,
                Eigen::Matrix<Scalar, 6, 1>* const rhs = nullptr) {
  // Total ops: 7

  // Input arrays

  // Intermediate terms (3)
  const Scalar _tmp0 = pose(3, 0) - tilt(0, 0);
  const Scalar _tmp1 = pose(4, 0) - tilt(1, 0);
  const Scalar _tmp2 = std::pow(isigma, Scalar(2));

  // Output terms (4)
  if (res != nullptr) {
    Eigen::Matrix<Scalar, 2, 1>& _res = (*res);

    _res(0, 0) = _tmp0 * isigma;
    _res(1, 0) = _tmp1 * isigma;
  }

  if (jacobian != nullptr) {
    Eigen::Matrix<Scalar, 2, 6>& _jacobian = (*jacobian);

    _jacobian.setZero();

    _jacobian(0, 3) = isigma;
    _jacobian(1, 4) = isigma;
  }

  if (hessian != nullptr) {
    Eigen::Matrix<Scalar, 6, 6>& _hessian = (*hessian);

    _hessian.setZero();

    _hessian(3, 3) = _tmp2;
    _hessian(4, 4) = _tmp2;
  }

  if (rhs != nullptr) {
    Eigen::Matrix<Scalar, 6, 1>& _rhs = (*rhs);

    _rhs.setZero();

    _rhs(3, 0) = _tmp0 * _tmp2;
    _rhs(4, 0) = _tmp1 * _tmp2;
  }
}  // NOLINT(readability/fn_size)

// NOLINTNEXTLINE(readability/fn_size)
}  // namespace sym
