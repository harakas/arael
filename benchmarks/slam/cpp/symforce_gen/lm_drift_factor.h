// -----------------------------------------------------------------------------
// This file was autogenerated by symforce from template:
//     function/FUNCTION.h.jinja
// Do NOT modify by hand.
// -----------------------------------------------------------------------------

#pragma once

#include <Eigen/Core>

namespace sym {

/**
 * Soft prior to the landmark initialization.
 *     jacobian: (3x3) jacobian of res wrt arg lm (3)
 *     hessian: (3x3) Gauss-Newton hessian for arg lm (3)
 *     rhs: (3x1) Gauss-Newton rhs for arg lm (3)
 */
template <typename Scalar>
void LmDriftFactor(const Eigen::Matrix<Scalar, 3, 1>& lm, const Eigen::Matrix<Scalar, 3, 1>& prior,
                   const Scalar isigma, Eigen::Matrix<Scalar, 3, 1>* const res = nullptr,
                   Eigen::Matrix<Scalar, 3, 3>* const jacobian = nullptr,
                   Eigen::Matrix<Scalar, 3, 3>* const hessian = nullptr,
                   Eigen::Matrix<Scalar, 3, 1>* const rhs = nullptr) {
  // Total ops: 10

  // Input arrays

  // Intermediate terms (4)
  const Scalar _tmp0 = lm(0, 0) - prior(0, 0);
  const Scalar _tmp1 = lm(1, 0) - prior(1, 0);
  const Scalar _tmp2 = lm(2, 0) - prior(2, 0);
  const Scalar _tmp3 = std::pow(isigma, Scalar(2));

  // Output terms (4)
  if (res != nullptr) {
    Eigen::Matrix<Scalar, 3, 1>& _res = (*res);

    _res(0, 0) = _tmp0 * isigma;
    _res(1, 0) = _tmp1 * isigma;
    _res(2, 0) = _tmp2 * isigma;
  }

  if (jacobian != nullptr) {
    Eigen::Matrix<Scalar, 3, 3>& _jacobian = (*jacobian);

    _jacobian.setZero();

    _jacobian(0, 0) = isigma;
    _jacobian(1, 1) = isigma;
    _jacobian(2, 2) = isigma;
  }

  if (hessian != nullptr) {
    Eigen::Matrix<Scalar, 3, 3>& _hessian = (*hessian);

    _hessian.setZero();

    _hessian(0, 0) = _tmp3;
    _hessian(1, 1) = _tmp3;
    _hessian(2, 2) = _tmp3;
  }

  if (rhs != nullptr) {
    Eigen::Matrix<Scalar, 3, 1>& _rhs = (*rhs);

    _rhs(0, 0) = _tmp0 * _tmp3;
    _rhs(1, 0) = _tmp1 * _tmp3;
    _rhs(2, 0) = _tmp2 * _tmp3;
  }
}  // NOLINT(readability/fn_size)

// NOLINTNEXTLINE(readability/fn_size)
}  // namespace sym
